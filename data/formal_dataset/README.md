# Formal Dataset Placeholder

This directory is reserved for the real labeled dataset.

Expected layout:

```text
formal_dataset/
  raw/
    images/
    masks/
  processed/
```

Use `scripts/prepare_formal_dataset.ps1` after placing the raw files.
