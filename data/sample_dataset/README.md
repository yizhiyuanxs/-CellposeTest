# Sample Dataset Placeholder

This is a placeholder dataset root for Stage A bootstrap validation.

Add real data using this structure:

```text
sample_dataset/
  train/
    images/
    masks/
  val/
    images/
    masks/
  test/
    images/
    masks/
```

Stage A allows these folders to stay empty so the project scaffold can be verified before real data arrives.
